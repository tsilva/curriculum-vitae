{
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "preinstall": "node -e \"const ua = process.env.npm_config_user_agent || ''; if (!ua.startsWith('pnpm/')) { console.error('Use pnpm for this repo.'); process.exit(1); }\"",
    "sentry:issues": "npm --prefix web exec -- tsx ../scripts/sentry-issues.ts",
    "links": "CHECK_EXTERNAL_LINKS=1 playwright test tests/smoke/links.spec.ts",
    "smoke": "playwright test",
    "security:check": "node scripts/check-dependency-sources.mjs && npm --prefix web audit --audit-level=low",
    "verify": "pnpm run security:check && npm --prefix web run lint && npm --prefix web run typecheck && npm --prefix web run build && pnpm run smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
